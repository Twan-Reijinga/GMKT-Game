class Room {
    constructor(Room, tileSize, tiles) {
        this.pattern = Room[map];
        this.tileSize = tileSize;
        this.height = roomPattern.length;
        this.width = roomPattern[0].length;
        this.tiles = tiles;
    }

    draw() {
        noSmooth
    }
}
